# ===========================================
# FlexSave Backend - Environment Variables
# ===========================================

# API Configuration
API_V1_PREFIX=/api/v1
ENVIRONMENT=development
DEBUG=true

# CORS - Origins autorisées (séparées par des virgules pour les arrays JSON)
CORS_ORIGINS=["http://localhost:3000","http://localhost:8080","http://127.0.0.1:3000"]

# ===========================================
# Firebase Configuration
# ===========================================

# ID du projet Firebase (visible dans Project Settings)
FIREBASE_PROJECT_ID=your-firebase-project-id

# Chemin vers le fichier service account JSON
# Téléchargé depuis: Firebase Console > Project Settings > Service accounts > Generate new private key
FIREBASE_SERVICE_ACCOUNT_PATH=./service-account.json

# ===========================================
# Stripe Configuration (Optionnel pour MVP)
# ===========================================

# Clé secrète Stripe (commence par sk_test_ ou sk_live_)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Clé publique Stripe (commence par pk_test_ ou pk_live_)
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Secret du webhook Stripe (commence par whsec_)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ===========================================
# Security
# ===========================================

# Clé secrète pour JWT (générer avec: openssl rand -hex 32)
SECRET_KEY=your-super-secret-key-change-this-in-production

# Algorithme JWT
ALGORITHM=HS256

# Durée de validité des tokens (en minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ===========================================
# Logging
# ===========================================
LOG_LEVEL=INFO
